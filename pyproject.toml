[tool.ruff]
target-version = "py311"
line-length = 120

[tool.ruff.lint]
select = ["E", "F", "W", "I"]

[tool.ruff.lint.per-file-ignores]
"app/services.py" = ["E402"]

[tool.pytest.ini_options]
testpaths = ["tests"]
